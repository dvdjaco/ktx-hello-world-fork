apiVersion: v1
kind: Pod
metadata:
  name: "hello-world-test-connection"
  labels:
    {{- include "helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: https://localhost:5000
  restartPolicy: Never
